#dynamic-git-config
zuul:
#默认以服务名为前缀。
  routes:
    api-a:
      path: /pro-dynamic/**
      serviceId: provider
    api-b:
      path: /con-dynamic/**
      serviceId: consumer
value4test: add_bus_kafka_1
